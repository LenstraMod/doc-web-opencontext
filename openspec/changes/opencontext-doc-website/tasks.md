## 1. Project Setup

- [x] 1.1 Initialize a new Next.js project using the App Router.
- [x] 1.2 Set up Tailwind CSS configuration and global styles.
- [x] 1.3 Install necessary dependencies: `@supabase/supabase-js`, `prismjs` (or similar for code highlighting).
- [x] 1.4 Create a `.env.local` template for Supabase credentials.

## 2. Shared Components

- [x] 2.1 Develop the `Navigation` component with links to the 4 main pages.
- [x] 2.2 Develop the `CodeColumn` component for displaying CLI commands.
- [x] 2.3 Develop the `TwoColumnLayout` component for the documentation page.
- [x] 2.4 Ensure the layout is responsive (mobile stacking).

## 3. Page Implementation

- [x] 3.1 Implement the `Home` page with hero section and value prop.
- [x] 3.2 Implement the `Docs` page using `TwoColumnLayout`.
- [x] 3.3 Implement the "Quick Start" section on the Docs page with the `git init` placeholder.
- [x] 3.4 Implement the `About Us` page with mission and story content.
- [x] 3.5 Implement the `Changelog` page layout.

## 4. Supabase Integration

- [x] 4.1 Initialize the Supabase client in a dedicated utility file.
- [x] 4.2 Create a service to fetch changelog entries from Supabase.
- [x] 4.3 Connect the `Changelog` page to the Supabase service.
- [x] 4.4 (Optional/Design) Add a simple form for user feedback if requested.

## 5. Documentation & Polish

- [x] 5.1 Generate the `doc.md` developer guide with project structure and Supabase setup.
- [x] 5.2 Add specific instructions in `doc.md` on how to manually update CLI commands.
- [x] 5.3 Review and refine the Tailwind CSS for a "premium" look and feel.
- [x] 5.4 Verify all links and responsive behavior.
